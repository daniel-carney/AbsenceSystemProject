/**
 * ABSENCE NOTIFICATION SYSTEM - CONTENT SCRIPT
 * This script runs in the background when the user visits designated 
 * project-related websites (like the syllabus landing page).
 */

console.log("Absence Notification System: Content Script Active.");

/**
 * FUTURE EXPANSION:
 * This script will eventually handle 'Deep Linking' logic.
 * It will listen for URL parameters (like ?key=BIO101) and 
 * automatically trigger the extension popup with the key pre-filled.
 */
